# CNR3 — DESIGNER REVIEW: DIAG.3c.1 patch — APPROVED (commit gated on final proof item)

**From:** designer/reviewer (W3D), via coordinator (W3X).
**Re:** `CMS07-DIAG.3c.1-plantrace-clean-end-capture.patch` (+ patch notes) against spec v2.2, scope v2,
decisions D3C1-A..F, and the committed post-DIAG.3b source.
**Verdict:** DIFF REVIEW **APPROVED**. No coder round-trip. Commit go-ahead pending ONE trivial proof-gate
item (final S8 content-body sample); everything load-bearing is green.

---

## 1. Verified COLD against source (not accepted from the notes)

- **Deletion scan:** the ONLY removed line in 1628 is the `CNR3_EDIT_VERSION` marker (sanctioned bump
  3b -> 3c.1). Zero proven lines touched — a clean R-PROCESS-25 patch.
- **Fence intact:** no added line touches any `cnr3_set_filter_error` site, dump-on-bail, Set 4 X/E, Set 5,
  a pin-list accessor, cache-core, or project files. (3c.2 material entirely absent.)
- **Invocation (D-1 reflex):** O = one RAII guard, instantiated once at top-level `arInitial`,
  `observe_open` in its destructor; R = exactly four `observe_result`, one per successful branch
  (cache_hit / pred_present / recovery / frame0); selftest carries a 2+2 synthetic fixture. All live.
- **O-guard bail-safety:** captures only when the slot started null AND `*frame_data` is now published;
  bail paths delete the *unpublished* frameData and leave `*frame_data` null, so the destructor
  early-returns on any bail — no use-after-free.
- **R ordering (all four branches):** copy facts (incl. `plantrace_ar_all_enter_tick`) BEFORE the discard,
  emit AFTER the successful discard using copied locals — pred_present copy@1260 < discard@~1268 <
  observe@1289; frame0 2272<2286<2305; recovery 2048<2052<2083; cache_hit copy->discard->observe->return.
  No read of freed `request_data`.
- **Spec v2.2 §8 invariant:** `enter_tick` sampled outside the lock (passed in); `action_seq =
  ++next_action_seq` bumped inside the `lock_guard`, same critical section as `push_back`. View (a) sorts
  `enter_tick ASC, action_seq ASC`, phase not a sort term.
- **Gates:** two-gate `#error`, sub-flags + window inside the feature `#if`, DSUM01-14 untouched,
  `#if/#endif` balanced; master gate off => all plantrace compiles out. Dump gated on PRINT, placed after
  D-SUM-14 before `freeNode`/`delete`, clean-end arm only (no bail arm), once by construction (free_filter
  runs once).
- **Window bound:** `from <= n <= to` enforced at both capture entries; buffer `reserve()`'d to `2*window`;
  `reserve_failed` tripwire prints an honest "records_may_be_incomplete" note rather than silently losing.
- Decisions D3C1-A..F all implemented; D3C1-D display tag ratified nonnumeric `[DSUM-PLANTRACE]`.

## 2. Proof-gate status

```text
Selftest four-way (all-on / macro-off / restored-all-on):        PASS  (56/56 x3 + 55/1 forced-fail per cfg;
                                                                        [DSUM-PLANTRACE] present->absent->present;
                                                                        DSUM06/07/09/14 unaffected by macro-off)
Plugin macro-off BYTE-IDENTICAL (R-PROCESS-19 exit gate)          PASS  S1 / S7 / S8 all fc /b identical
  via A/B harness (plantrace OFF vs ON, fc /b y4m):                     (S8 required a harness fix: the S8 .vpy
                                                                        shuffle was unseeded -> A and B ran
                                                                        different orders; seeded, S8 now identical.
                                                                        NOT a plantrace defect.)
Content sanity S1 (in-order control):                            PASS  10 O + 10 R paired; FRAME0 + PRED_PRESENT,
                                                                        no recovery/cache-hit; branch-specific
                                                                        sources=[n]; branch-derived pinned
                                                                        (frame0=[], pred=[n-1]); machine codes with
                                                                        honest overlap; *=grid glyph; fixed-20 tick;
                                                                        monotonic action_seq; 3 legend-headed views;
                                                                        window respected.
Content sanity S8 (recovery):                                    PENDING eyeball of one RECOVERY_EXACT + one
                                                                        CACHE_HIT record body (observe-only already
                                                                        PASS; recovery capture fired). Trivial;
                                                                        expected PASS.
```

Note: the plantrace window captured ~[0,120] (a subset), while D-SUM-12 is the full-run aggregate (S8: 181
recovery plans / 800 frames / 22.625%, internally consistent). Windowed-plantrace-vs-full-run-D-SUM-12 count
reconciliation would need a full-window run; it is an OPTIONAL deeper check, not a 3c.1 gate. 3c.1's job is
record structure + observe-only, both of which are proven.

## 3. Commit

APPROVED to commit once the S8 record-body sample confirms (expected). At commit:
- Seven source files + `cnr3_build_config.h` (new family gates + the marker).
- `CNR3_EDIT_VERSION` marker (paste-ready, pre-commit working-tree string):
  `CMS07-DIAG.3c.1-plantrace-clean-end-capture`
- Commit message notes: DIAG.3c.1 observe-only plan-trace capture + clean-end dump; decisions D3C1-A..F;
  nonnumeric `[DSUM-PLANTRACE]`; observe-only proven by A/B byte-identical (S1/S7/S8); the S8 harness seed
  fix (test methodology, not plugin).

## 4. Carried forward (not 3c.1)

- 3c.2 = dump-on-bail + Set 4 E/X + Set 5 failure-reason across the 65 bail sites (invasive, R-PROCESS-21/25,
  site-to-category table as foundation).
- The S8 `.vpy` unseeded-shuffle fix (`random.seed`) is now a standing harness improvement — reproducibility
  that A1 (plan-trace analysis) will also rely on.
