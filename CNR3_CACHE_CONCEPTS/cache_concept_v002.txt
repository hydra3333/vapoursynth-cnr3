Regarding a possible internal output-frame cache management solution for cnr3, "Proposal A".

I am not saying Proposal A is even possible let alone workable,
since I have gaps in understanding how Vapoursynth (VS) and our cnr3 dll inter-operate
and it may not be feasible nor workable.

I know that cnr3 currently uses VS mode fmUNORDERED whereby output frames can be requested by VS
out of numerical order however it guarantees that only one call to cnr3_get_frame() occurs at a time.

Apparently, in mode fmUNORDERED it is possible that calls from VS to cnr3 include;
cnr3_get_frame(output_frame_number, arInitial) 
cnr3_get_frame(output_frame_number, arAllFramesReady)

which I think may conceptually be along the lines -
arInitial: 
1. VS calls cnr3_get_frame(output_frame_number, arInitial) with the output frame number for cnr3 to calculate and return.
2. cnr3_get_frame assesses what SOURCE frame numbers it needs and
   - makes individual calls to requestFrameFilter(requested_source_frame_number)
   - then returns ?something? to VS as the return code
VS:
3. does what it needs including SOURCE frame caching to get the requested SOURCE frame(s) and
   get them ready to pass into cnr3 by a call to cnr3_get_frame(output_frame_number, arAllFramesReady)
arAllFramesReady:
4. VS calls cnr3_get_frame(output_frame_number, arAllFramesReady) to say that the
   previously requested SOURCE frames are ready
5. cnr3_get_frame() retrieves the now-available SOURCE frames from VS using calls to getFrameFilter()
   then uses the retrieved SOURCE frame(s) to calculate the OUTPUT frame requested by VS,
   then releases the SOURCE frames,
   then returns the OUTPUT frame and probably a return code.

We are considering moving away from mode fmUNORDERED which is currently used by cnr3,
as it also requires "-r 1" on the vspipe commandline, eg "vspipe.exe -r 1 ...",
which means we are instead a leaning toward multithreading modes, with mode fmUNORDERED only
being a current stepping stone.

I gather instances of cnr3 are independent, eg 
a) one instance to process SOURCE A and retiurn output frams for SOURCE A,
b) and another instance to process SOURCE B and return output frames for SOURCE B
where the 2 instances are independent and never see each other's internal data except
perhaps to obtain an instance number or something.
We must ensure instances are completely separated.

What we need is for all threads for an instance (if such a concept/thing exists, see below)
to share the "global data" in that instance, eg a "common shared cache" of temporarily stored output-frames
and perhaps some cache lists and controlling variables and whatnot which pertain just to that instance.
and more importantly not to see anything in the OTHER instance. 
I was told cnr3 currently does that however we need to enforce it and state it clearly.
Mutexes will certainly also be required to manage it.

Various VS modes exist. Here is a breakdown of three relevant modes:
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
++++++++++
**fmUnordered**
- Only one call to `cnr3_get_frame()` active at any time, for either activation reason
- No concurrency whatsoever within the filter
- Frames can still be requested out of order by VS
- That is why `-r 1` is currently needed - it prevents VS from queuing multiple requests simultaneously,
  which would otherwise cause the out-of-order rejection to fire constantly
- Simplest to reason about, but single-threaded by design
++++++++++
++++++++++
**fmParallelRequests**
- Multiple concurrent `cnr3_get_frame(N, arInitial)` calls are allowed simultaneously, each in their own thread
- Only **one** `cnr3_get_frame(N, arAllFramesReady)` is active at any time - serialised by VS
- This is the most natural fit for Proposal A because:
    - Parallel arInitial calls allow multiple threads to be prefetching source frames from VS simultaneously, warming the VS source cache efficiently
    - Serialised arAllFramesReady means all actual computation and cache writes happen one at a time, removing the concurrent-write race problem entirely
    - The mutex is still needed to protect arInitial cache reads from concurrent arAllFramesReady writes, but the heavy computation path is never concurrent
++++++++++
++++++++++
**fmParallel**
- Both `arInitial` and `arAllFramesReady` calls can run concurrently in multiple threads simultaneously
- Maximum throughput potential but maximum complexity
- For Proposal A this means two threads could both be in arAllFramesReady simultaneously,
  both computing hole-fills, both attempting cache writes - the mutex handles the writes but the
  redundant parallel computation of the same holes is wasteful
- The serial recursive dependency (`output[N]` needs `output[N-1]`) also makes truly parallel
  arAllFramesReady calls difficult to exploit usefully for CNR3 specifically
++++++++++
++++++++++
**Summary for Proposal A:**
| Mode | arInitial | arAllFramesReady | Fits Proposal A |
|---|---|---|---|
| fmUnordered | serialised | serialised | works, but needs -r 1 |
| fmParallelRequests | concurrent | serialised | best fit |
| fmParallel | concurrent | concurrent | workable but complex |

`fmParallelRequests` is the recommended upgrade target - it gives you parallel VS source cache warming
for free while keeping the recursive computation path serial, which matches the algorithm's fundamental nature.
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
## Instance isolation in VapourSynth and CNR3

**How VS instances work:**

When a VapourSynth script calls `cnr3.CNR3(clipA)` and `cnr3.CNR3(clipB)`, VS calls `cnr3_create()` twice.
Each call produces a completely independent `Cnr3Data` object allocated on the heap via `new Cnr3Data(...)`,
and VS stores a pointer to each one as `instanceData`.
Every subsequent call to `cnr3_get_frame()` and `cnr3_free()` receives only that instance's own pointer - VS never mixes them.
The two instances are structurally incapable of seeing each other's data through the VS interface.

**How CNR3 currently enforces this:**

It already does - `Cnr3Data` contains everything per-instance:
- the node pointer
- all parameters
- all lookup tables
- the `Cnr3CacheManager` with `prev_output` and `next_needed`
- the `instance_id` (read-only after creation, so no sharing needed)

The atomic `g_cnr3_next_instance_id` is the only truly global state, and it is only ever used once per instance at creation time to assign the ID, so it is safe.

---

**What changes under mode fmParallelRequests:**

Under `fmUnordered` instance isolation was trivially guaranteed because only one thread ever touched a given instance at a time. 
Under `fmParallelRequests` multiple threads can now call `cnr3_get_frame(N, arInitial)` concurrently for the same instance.
They all receive the same `instanceData` pointer - meaning they all share the same `Cnr3Data` object and
everything in it including the output frame cache.

This is correct and desired - those threads are supposed to share the cache.
But it means the cache and its associated metadata become a shared resource
requiring mutex protection, which Proposal A already accounts for.

---

**What needs to be stated explicitly:**

```
Instance isolation invariants for CNR3 under fmParallelRequests:

1. Each CNR3 filter instance owns exactly one Cnr3Data object, created in
   cnr3_create() and destroyed in cnr3_free(). VS guarantees that instanceData
   passed to cnr3_get_frame() and cnr3_free() always points to that instance's
   own Cnr3Data and never to another instance's.

2. No static or global mutable state is shared between instances at runtime.
   g_cnr3_next_instance_id is the sole global variable and is only written
   once per instance at creation time using an atomic operation.

3. All per-instance state - parameters, lookup tables, output frame cache,
   checkpoint table, cache metadata, and controlling variables - lives inside
   Cnr3Data and is invisible to all other instances.

4. All threads serving a given instance share that instance's Cnr3Data,
   which is the intended design. Access to any mutable field within Cnr3Data
   (particularly the output frame cache and its metadata) must be protected
   by a mutex that is itself a member of Cnr3Data, ensuring the mutex is
   also per-instance and never shared across instances.

5. Threads serving instance A and threads serving instance B never contend
   on the same mutex, never read or write each other's cache, and never
   interact in any way.
```

The key design point for point 4 is that the mutex must live **inside** `Cnr3Data`, not as a global or static.
A global mutex would accidentally serialise across instances, defeating the purpose of having independent instances.
A mutex inside `Cnr3Data` is automatically scoped to that instance.
+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++


To be clear about cnr3 filtering calculations, to calculate an OUTPUT frame [N],
the inputs to that process are
(a) SOURCE frame [N], blended with
(b) OUTPUT frame [N-1]
which means, as you can see, that calculation of OUTPUT frame [N] actually
depends on all prior OUTPUT frames [0]..[N-1] being pre-calculated.
In the out-of-order vapoursynth OUTPUT frame request model, this seems almost impossible to guarantee.
Pragmatically (and yes edge cases exist at start frame [0] etc) 
perhaps we can assume that calculation of OUTPUT frame [N] depends on the pre-calculation
of only OUTPUT frames [N-50]..[N-1]. This still leaves an interesting circumstance
in a world of out-of-order (and parallel as well in a multi-threading world) calls.

The challenge is to have some satisfactory level of "performance" without horrendous levels
of re-calculation for each and every OUTPUT frame. Even at the expense of some extra memory usage.
Hence a wondering if a separate per-instance CACHE/CHECKPOINT type of approach having
prior pre-calculated OUTPUT frames may be an answer. It offers quick access to pre-calculated frames
at the expense of dealing with a cache and having frame gaps and having some levels of re-calculation,
and complexity, without the cpu cost of a full re-calculation every time.
Say a flexible-size rolling cache of 50 OUTPUT frames and an OUTPUT frame being 
marked as a "checkpoint frame" (not pruned as much as non-cahce frames) every 10 frames. 
(all numbers being subject to change as we go).

For cnr3,
- the by-far normal use-case (U1) would be for vspipe to process the script referencing cnr3 and streaming
calculated script FINAL OUTPUT frames (in order, managed by vapoursynth) into ffmpeg 
for encoding into a watchable video.
- a secondary, and hopefully very rare, use case (U2) would be a user feeding the script 
into a video viewer where the user can jump around in the timeline, thus causing large "jumps"
in requested OUTPUT frame numbers, and consequently hurting the cache concept - which must still
survive in that environment, even if with very degraded performance.
- to be clear, our focus is on U1 even though VS delivers OUTPUT frame requests out of frame-number order
and/or in parallel.


Here's a setup for discussion on how the cacheing works (excluding management and pruning, which can be discussed separately)
Assume use case U1 which somehow starts at frame 0 and staggers upward.
We know that even fmUNORDERED with "-r 1" we may have source filters such that VS (vapoursynth)
could still deliver frame requests in any order, even if "close-ish".
For the sake of this examination lets say it's staggered up to requesting 
OUTPUT frames in this order:  80 71 79 72 78 73 77 64 75 76 etc 
(yes, near frame 0 and maybe near end-frame would be edge cases).

Notes:
```
VS requestFrameFilter() requests one frame per call; 
VS getFrameFilter() retrieves one frame per call; 
cnr3 cnr3_get_frame(... arAllFramesReady ...) returns one output frame.
VapourSynth documents requestFrameFilter, getFrameFilter, releaseFrameEarly, cacheFrame, and setFilterError
as the relevant inside-filter frame functions, and the filter callback itself returns a single frame pointer"

Multiple duplicate source-frame requests into VS may be much cheaper than decoding/copying again if the
source frame is already in VapourSynth’s cache.
Great, hopefully we can work with this assumption:
That multiple requests from multiple concurrent cnr3_get_frame(output_frame_number, arInitial) to VS for a source frame are very "cheap" even when duplicated.
So if one requests say 50 previous source frames (to pre-calculate temporary OUTPUT frames  to [N-1] (note: we want to avoid all that re-calculation by caching)
then there will be substantial overlaps in requested SOURCE frames from each concurrent cnr3_get_frame(output_frame_number, arInitial) ...
HOWEVER these are "cheap" since vapoursynth may have cached almost all of those source frames.

For example, if request for output frame 80 causes CNR3 to request source frames 30..80,
and a later request for output frame 79 requests source frames 29..79, 
VapourSynth may already have many of those source frames cached.

CNR3 should not assume the VS source cache will always hold everything it wants,
especially if many overlapping ranges are requested. 
We will have to just wear that cost.
Perhaps we could also tell users to set VS memory max to 8Gb or 16Gb and see if that "helps".

VS mode fmUNORDERED and "vspipe.exe -r 1" and is fragile for "large jumps".
We hope to perhaps use checkpointed cached frames to assist in recalculations by only needing to
re-start pre-OUTPUT-frame calculations at a nearest prior checkpointed OUTPUT frame.
The proposed OUTPUT frames cache seems very likely to have prior relevant OUTPUT frame cached
Perhaps thrashing may occur around the prevailing current set of requests being unordered,
and/or in multi-threading where they will definitely be processed unordered and mutexes are needed.

Although if we expect the standard use case U1, generally it would be a form of rolling "back-cache"
of OUTPUT frames with checkpointed OUTPUT frames every so-often which would be used as starting points
to help re-calculate "cache holes" without going back too far and recalculating a lot of frames a lot of times.
Maybe the startup cost perhaps may be high, who knows yet without seeing in real life what happens in testing.

Re the serial recursive nature of cnr3,
"we cannot safely compute OUTPUT[74] from SOURCE[74] alone, we need a valid predecessor chain of OUTPUT frames."
Yes, those are likely to be in the OUTPUT frame cache, however if they are missing
then go back to the nearest predecessor checkpoint and calculate from there,
which can easily be done given it would already have requested and retrieved most if not all
of the corresponding source frames in order to calculate and fill the OUTPUT frame cache holes
and establish "less-cache-pruned" checkpointed OUTPUT frames which can be used as starting points.


Query arund VS modes.
fmUnordered ... I had an impression that other modes allowed parallel calls to
cnr3_get_frame(output_frame_number, arInitial) and cnr3_get_frame(output_frame_number, arAllFramesReady)
which seem as if they may work well enough in the proposed "Proposal A".
I would like a confirmed clarification of what other modes do.

-----------------------------------------------------------------------------------------------------

I know the "Proposal A" below is incomplete and has faults and limitations.
It may not even be understandable.
Lets see if you can make some sense of it and frame it more understandably in light of the above.

A clarification: I have also got it wrong in where I indicated that 
processing inside cnr3_get_frame(output_frame_number, arAllFramesReady) we only retrieved the SOURCE frames
we wanted rather than all of those requested in the prior cnr3_get_frame(output_frame_number, arInitial)
since I gather we apparently have to make calls to explicitly "get" and "free" them and thus not leave any dangling;
thankfully the retrieval of a SOURCE frame from VS is only about pointers and not copying a huge amount
of frame pixel data and whatnot, and a call to freeing is cheap.


"Proposal A"
------------
```
If I understand it correctly, the VS model for calling cnr3 to return a processed OUTPUT frame is conceptually along the lines:
arInitial: 
1. VS calls cnr3_get_frame(output_frame_number, arInitial) with the output frame number for cnr3 to calculate and return.
2. cnr3_get_frame assesses what SOURCE frame numbers it needs and
   - makes individual calls to requestFrameFilter(requested_source_frame_number)
   - then returns ?something? to VS as the return code
VS:
3. does what it needs including SOURCE frame caching to get the requested SOURCE frame(s) and
   get them ready to pass into cnr3 by a call to cnr3_get_frame(output_frame_number, arAllFramesReady)
arAllFramesReady:
4. VS calls cnr3_get_frame(output_frame_number, arAllFramesReady) to say that the
   previously requested SOURCE frames are ready
5. cnr3_get_frame() retrieves the now-available SOURCE frames from VS using calls to getFrameFilter()
   then uses the retrieved SOURCE frame(s) to calculate the OUTPUT frame requested by VS,
   then releases the SOURCE frames,
   then returns the OUTPUT frame and probably a return code.

If we assume VS manages its cache VERY well, then 
(a) when a call is made from VS to cnr3_get_frame(output_frame_number, arInitial) for output frame 80
cnr3_get_frame could request the prior 50 source frames and return and then 
(b) say the next cnr3_get_frame(output_frame_number, arInitial) is for frame 79,
the it too could request the prior 50 source frames and return.

In practise with VS doing its cacheing well, for (b) there may be very little
extra work being done to retrieve source frame(s) because most if not all of the
source frames are in already the VS SOURCE frame cache arising from the previous call.
That assumes that VS works with pointers to the readonly source frames
rather than expensively copying and returning a copy of the data in the source frames itself,
and gives each cnr3_get_frame(output_frame_number, arAllFramesReady) the pointer(s)
to the requested fames in cache rather than to a copy of the large source frame data.

We could have a cnr3 cache of OUTPUT frames already calculated (say 50 deep but we'll tune that)
and in that cache every 10th frame (perhaps every one divisible by 10 ?, tunable later)
is marked as a checkpoint output frame and not pruned by a cache cleaning process
unless it has deemed the checkpoint frame is no longer useful).

Let's assume we put a mutex on our cache/lists whilst checking it or updating it.

**********************************************************************************************
CRITICAL NOTE:

ABSOLUTELY ALL OF
    - cache frame search and lookup
    - checkpoint frame search and lookup 
    - anything else touching the frame/checkpoint cache or related items
MUST STRICTLY respect and use only "frame number order" at all times.
NO EXCEPTIONS.

Checkpoint lookup MUST always be:
    the checkpoint with the highest frame number that is strictly
    less than the requested output frame N.

It is absolutely forbidden to use any other order such as insertion order or recency of write,
since it will definitely break multi-threaded cacheing logic.
**********************************************************************************************

We know that fmUNORDERED with "-r 1" could have source filters which mean that VS (vapoursynth)
could still deliver OUTPUT frame requests in any order, for the sake of this examination lets say
it's got up to requesting in order  80 71 79 72 78 73 77 64 75 76 etc 
... near frame 0 and near end-frame would be edge cases.

Notionally for (a) (calculating OUTPUT frame 80),

arInitial: (which only identifies and makes SOURCE frame requests, does not retrieve any)
- {sets mutex}
- Identify most recent checkpointed OUTPUT frame in the cache which is just prior to
  the requested OUTPUT frame
- {releases mutex}
- makes individual calls to VS requestFrameFilter() for 
  * SOURCE frames covering the full range back to the most recent checkpointed
    OUTPUT frame in the cache which is just prior to the requested OUTPUT frame
    if prior-checkpoint-found then perhaps request the last X frames we decided on earlier (50?) and in arAllFramesReady fetch/calculate those and checkpoints
  * and the SOURCE frame 80 itself

- returns

  --- arInitial returns here; VS prepares source frames; callback arAllFramesReady eventually begins ---

arAllFramesReady: (which only retrieves and releases frames requested in arInitial)

- makes individual calls to VS getFrameFilter() to retrieve 
  * SOURCE frames covering the full range back to the most recent checkpointed
    OUTPUT frame in the cache which is just prior to the requested OUTPUT frame
  * and the SOURCE frame 80 itself
- {sets mutex}
- checks the OUTPUT frame cache to identify holes in the full range back to the most recent
  checkpointed OUTPUT frame in the cache which is just prior to the requested OUTPUT frame
- {releases mutex}
- (re)calculates OUTPUT frames for the identified holes using
  relevant SOURCE frames retrieved and relevant cached OUTPUT frames,
  IN ASCENDING FRAME ORDER since each output frame depends on the previous one 
- {sets mutex}
- fills the holes with the calculated OUTPUT frames, only if not
  already filled by another thread, updates checkpoints
- {releases mutex}
- calculates OUTPUT frame 80
- {sets mutex}
- stores the newly calculated OUTPUT frame 80 into the cache, only
  if not already filled by another thread, updates checkpoints
- {releases mutex}
- releases all SOURCE frames retrieved via getFrameFilter()
- returns OUTPUT frame 80 to VS

Note: the mutex may be acquired and released several times during the above
process, for example when re-checking before storing a frame into the cache.

Notionally for (b) (calculating OUTPUT frame 79),

arInitial:
- mirrors frame 80's arInitial exactly including requestFrameFilter()
  Identify most recent checkpointed OUTPUT frame in the cache which is just prior to
  the requested OUTPUT frame
  Makes individual calls to VS requestFrameFilter() for 
  * SOURCE frames covering the full range back to the most recent checkpointed
    OUTPUT frame in the cache which is just prior to the requested OUTPUT frame
    if prior-checkpoint-found then perhaps request the last X frames we decided on earlier (50?) and in arAllFramesReady fetch/calculate those and checkpoints
  * and the SOURCE frame 79 itself
  In practice most or all of those source frames are already in VS's source
  cache from the prior frame 80 arInitial, so the extra VS-side work is minimal.

  --- arInitial returns here; VS prepares source frames; callback arAllFramesReady eventually begins ---

arAllFramesReady:
- the process is exactly per (a) ...
- makes individual calls to VS getFrameFilter() to retrieve 
  * SOURCE frames covering the full range back to the most recent checkpointed
    OUTPUT frame in the cache which is just prior to the requested OUTPUT frame
  * and the SOURCE frame 79 itself
- by this point the output frame cache has largely been filled by the frame 80
  arAllFramesReady processing, so perhaps none or only one output frame may be missing
- {sets mutex}
- checks the OUTPUT frame cache to identify holes in the full range back to the most recent
  checkpointed OUTPUT frame in the cache which is just prior to the requested OUTPUT frame
- {releases mutex}
- (re)calculates OUTPUT frames for the identified holes using
  relevant SOURCE frames retrieved and relevant cached OUTPUT frames,
  IN ASCENDING FRAME ORDER since each output frame depends on the previous one 
- {sets mutex}
- fills the holes with the calculated OUTPUT frames, only if not
  already filled by another thread, updates checkpoints
- {releases mutex}
- calculates OUTPUT frame 79
- {sets mutex}
- stores the newly calculated OUTPUT frame 79 into the cache, only
  if not already filled by another thread, updates checkpoints
- {releases mutex}
- releases all SOURCE frames retrieved via getFrameFilter()
- returns OUTPUT frame 79 to VS

cnr3 Cache management including checkpoint management and pruning is yet to be identified and considered.
Allowing temporary overflow of cache size (say double) may be necessary.

What happens with a human using the script to visually view and jump around
(creating "large" distances between requested output frames) is an issue perhaps 
dealt with by overflow cache size.

In the normal expected use case (linear frame access, deliver output frames to ffmpeg for encoding)
even with out of order frame deliver as known by fmUNORDERED
I  guess the main slowness may perhaps be caused by cache filling in near the start.
```

**********************************************************************************************
APPENDIX A - instrumentation and comparison of VS thread modes in action
**********************************************************************************************

About the development and instrumentation strategy:

---

## Development strategy

**Phase 1 - VS mode fmParallelRequests**
Develop and validate correctness here first.
The serialised arAllFramesReady means most races are structurally impossible,
so the mutex logic and cache correctness can be validated without needing
to reason about concurrent computation. 
Get benchmarks here as the baseline.

**Phase 2 - VS mode fmParallel**
Add/change code
    - mode flag to fmParallel
    - addition of condition variables for the "waiting on a frame being computed" case
    - related debug print code 
Switch the mode flag, run the same test suite and benchmarks.
Any correctness failures that appear are by definition concurrency bugs that were
latent in Phase 1 but masked by serialisation.
The instrumentation in pllace from Phase 1 will immediately show where
contention and wasted computation occur.

This is a clean methodology, the code changes between phases are minimal
    - mode flag
    - addition of condition variables for the "waiting on a frame being computed" case
and the instrumentation carries across unchanged.

---

## Instrumentation design

**On numbered debug statements:**
Yes, useful, and yes possible.
The pattern is a scoped debug category system rather than a flat sequence of numbers.
Each debug print carries:
- instance ID (already in CNR3)
- frame number being processed
- thread identifier
- category tag
- the statistic or event

Something like:
```
CNR3 [inst=1] [frame=080] [thr=A] [CACHE_HIT]    output[74] found in cache, skipped computation
CNR3 [inst=1] [frame=080] [thr=A] [CACHE_MISS]   output[74] not in cache, computing
CNR3 [inst=1] [frame=080] [thr=A] [CACHE_RACE]   output[74] computed but already filled by another thread, discarded
CNR3 [inst=1] [frame=080] [thr=A] [CACHE_WRITE]  output[74] written to cache
CNR3 [inst=1] [frame=080] [thr=A] [CKPT_PROMOTE] output[80] promoted to checkpoint
CNR3 [inst=1] [frame=080] [thr=A] [CKPT_ANCHOR]  using checkpoint output[70] as anchor
CNR3 [inst=1] [frame=080] [thr=A] [HOLE_COUNT]   3 holes identified in range [71..80]
CNR3 [inst=1] [frame=080] [thr=A] [SRC_REQUEST]  requesting source frames [71..80]
CNR3 [inst=1] [frame=080] [thr=A] [SRC_RETRIEVE] retrieving source frames [71..80]
CNR3 [inst=1] [frame=080] [thr=A] [OUT_RACE]     output[80] computed but already in cache, discarded
CNR3 [inst=1] [frame=080] [thr=A] [OUT_WRITE]    output[80] written to cache
CNR3 [inst=1] [frame=080] [thr=A] [FRAME_DONE]   output[80] returned to VS
```

The category tags make it easy to grep for specific event types across a full log:
```
grep CACHE_RACE cnr3.log | wc -l
grep OUT_RACE   cnr3.log | wc -l
```

---

## Specific statistics worth capturing per-frame and per-run

**Cache efficiency:**

| Statistic | Tag | What it tells you |
|---|---|---|
| Holes identified in range | `HOLE_COUNT` | How sparse the cache is at request time |
| Holes already filled by recheck | `HOLE_PREFILLED` | Benefit of concurrent arInitial prefetching |
| Holes computed then discarded (race) | `CACHE_RACE` | Wasted computation under fmParallel |
| Holes filled successfully | `CACHE_WRITE` | Actual cache contribution |
| Output frame computed then discarded | `OUT_RACE` | Wasted final computation under fmParallel |
| Checkpoint anchor used | `CKPT_ANCHOR` | Which checkpoint was chosen and how far back |
| Checkpoint promoted | `CKPT_PROMOTE` | Checkpoint promotion rate |
| Cache size at request time | `CACHE_SIZE` | Rolling cache occupancy |

**Source frame efficiency:**

| Statistic | Tag | What it tells you |
|---|---|---|
| Source frames requested | `SRC_REQUEST` | Request volume per output frame |
| Source frame range width | `SRC_RANGE` | How far back each arInitial reaches |

**Threading visibility:**

| Statistic | Tag | What it tells you |
|---|---|---|
| Thread ID on every event | all tags | Lets you reconstruct per-thread timelines |
| Mutex wait (if measurable) | `MUTEX_WAIT` | Contention hotspots |

---

## Per-run aggregate summary

At `cnr3_free()` time, print a summary block:

```
CNR3 [inst=1] [SUMMARY] frames_processed=500
CNR3 [inst=1] [SUMMARY] total_holes_identified=312
CNR3 [inst=1] [SUMMARY] holes_prefilled_by_recheck=287
CNR3 [inst=1] [SUMMARY] holes_computed=25
CNR3 [inst=1] [SUMMARY] holes_discarded_race=3
CNR3 [inst=1] [SUMMARY] output_discarded_race=1
CNR3 [inst=1] [SUMMARY] checkpoint_anchors_used=48
CNR3 [inst=1] [SUMMARY] avg_anchor_distance=8.3
CNR3 [inst=1] [SUMMARY] cache_hit_rate=91.9%
CNR3 [inst=1] [SUMMARY] wasted_computation_rate=0.8%
```

These numbers directly answer the fmParallelRequests vs fmParallel comparison question,
specifically `holes_discarded_race`, `output_discarded_race`, and `wasted_computation_rate`
should be zero or near-zero under fmParallelRequests and nonzero under fmParallel.

---

## On numbered statements specifically

Sequential numbers on individual debug lines are less useful than category tags because:
- numbers tell you nothing without looking up a legend
- tags are self-documenting in the log
- tags survive code reorganisation without renumbering

However a **category registry** in the source
    - a header or comment block listing all tags and their meanings
serves the same documentation purpose as a numbered legend and is more maintainable.
Something like:
```cpp
// CNR3 debug category registry
// CACHE_HIT     output frame found in cache, computation skipped
// CACHE_MISS    output frame not in cache, computation required
// CACHE_RACE    output frame computed but already filled by another thread
// CACHE_WRITE   output frame successfully written to cache
// CKPT_ANCHOR   checkpoint selected as computation anchor
// CKPT_PROMOTE  output frame promoted to checkpoint
// HOLE_COUNT    number of holes identified in range at arAllFramesReady entry
// HOLE_PREFILLED hole found filled on recheck, skipped
// OUT_RACE      requested output frame computed but already in cache
// OUT_WRITE     requested output frame written to cache
// SRC_REQUEST   source frame range requested in arInitial
// SRC_RETRIEVE  source frame range retrieved in arAllFramesReady
// MUTEX_WAIT    thread waited on mutex (if instrumented)
// FRAME_DONE    output frame returned to VS
// SUMMARY       per-instance aggregate at filter teardown
```

This gives you the documentation benefit of numbered statements
with the grep-ability and self-documentation of named tags.


**********************************************************************************************
**********************************************************************************************
APPENDIX B - CACHE MANAGEMENT for CNR3
**********************************************************************************************

# GLOBAL TUNABLE CONSTANTS
All tunable parameters are global constants defined in one place in the source.
    cache_capacity          = 50    // nominal non-checkpoint pool frame count
    overflow_factor         = 1.1   // non-checkpoint pool overflow multiplier (default gives 55 frames)
    checkpoint_interval     = 10    // frames between checkpoint promotions
    checkpoint_max_retain   = 8     // maximum checkpoints in checkpoint pool
    checkpoint_min_retain   = 3     // minimum checkpoints always retained

# STRUCTURE
Primary structure: segmented cache with sliding window
Non-checkpoint pool:
    capacity:       cache_capacity (default 50, tunable)
    overflow:       up to cache_capacity * overflow_factor (default 1.1x = 55)
    eviction order: furthest behind current frontier first (lowest frame number first)
    eviction trigger: exceeding overflow capacity

Checkpoint pool:
    capacity:       checkpoint_max_retain (default 8, tunable)
    minimum retain: checkpoint_min_retain (default 3, tunable)
    eviction order: lowest frame number first
    eviction trigger: exceeding checkpoint_max_retain
    hard constraint: never evict if it would leave fewer than checkpoint_min_retain
    hard constraint: never evict the two checkpoints nearest to current frontier
    special case:   frame 0 checkpoint retained permanently until teardown

The frontier is defined as the highest frame number currently in either pool.

# FRAME STORAGE
Do we store VS-managed output frame pointers or our own separate data?
    Use VS-managed output frame pointers.
    VS output frames are reference-counted heap objects managed by VS.
    When CNR3 calls vsapi->addFrameRef(frame) it increments the reference count and gets back a pointer it can hold onto.
    VS will not free that frame while CNR3 holds the reference.
    When CNR3 calls vsapi->freeFrame(frame) it decrements the count and VS frees it when the count hits zero.
    So the CNR3 cache stores VS-managed frame pointers held open by addFrameRef - not copies, not separate pixel buffers.
    CNR3's cache is essentially two ordered lists of (frame_number, VSFrame*) pairs where each pointer is kept alive
    by an outstanding addFrameRef call, one list for non-checkpoint frames and one for checkpoint frames.
    This is the least impact approach - no pixel copying, no separate memory management,
    VS handles the actual frame data lifetime.
    No per-slot "currently being computed" status flag or condition variable is needed under
    fmParallelRequests - the conditional store under mutex is sufficient.
    These may be revisited if and when fmParallel is implemented.

# IMPLEMENTATION DATA STRUCTURES
Non-checkpoint pool:
    std::map<int, VSFrame*> keyed by frame number
    ordered by frame number automatically, making lowest-first eviction O(1)

Checkpoint pool:
    std::map<int, VSFrame*> keyed by frame number
    same ordering property, lowest-first eviction O(1)

Cache index (for fast lookup of either pool):
    std::unordered_map<int, VSFrame*> keyed by frame number
    O(1) average lookup to check if a frame is cached before computation

Per-instance mutex:
    std::mutex as a member of Cnr3Data
    never a global or static mutex

Frontier tracking:
    int highest_cached_frame_number as a member of Cnr3Data
    updated whenever a frame is written to either pool
    updated downward if the frontier frame is evicted

# PRUNING POLICY
Note: the term "checkpoint" is used throughout in preference to "anchor".
A checkpoint frame is the starting point from which a forward computation chain is built.

Never evict:
    - the two checkpoints with the highest frame numbers below the current frontier
    - frame 0 checkpoint (permanent)
    - any frame currently held as a live anchor by an active computation thread
      (addressed by per-slot reference count under fmParallel, not needed under fmParallelRequests)

Under fmParallel the per-slot reference count is an integer active_readers per cache slot,
incremented when a thread takes that frame as a computation anchor and decremented when the
thread is done with it. The pruning pass skips any slot where active_readers > 0.
This is not needed under fmParallelRequests and should not be implemented until
fmParallel development begins.

Evict first (non-checkpoint pool):
    - furthest behind frontier first (lowest frame number first)
    - triggered when non-checkpoint pool exceeds overflow capacity

Evict last (checkpoint pool):
    - lowest frame number first
    - triggered only when checkpoint pool exceeds checkpoint_max_retain
    - subject to never-evict constraints above

# CHECKPOINT POLICY
Promotion:     at write time, if frame_number % checkpoint_interval == 0
Interval:      checkpoint_interval = 10 (tunable global constant)
Max retained:  checkpoint_max_retain = 8 (tunable global constant)
Min retained:  checkpoint_min_retain = 3 (tunable global constant)
Frame 0:       always checkpoint, never evicted
Eviction:      lowest frame number first, subject to min_retain and never-evict constraints

# STARTUP
Frame 0:
    computed from source only, no blend with predecessor
    immediately promoted to checkpoint
    never evicted
Frames 1 through [checkpoint_interval - 1]:
    computed normally using frame 0 as the chain anchor (checkpoint)
    non-checkpoint frames, subject to normal pruning
Frame checkpoint_interval (e.g. frame 10):
    first non-zero checkpoint promoted at write time
    normal checkpoint retention rules apply from here

# SEEK BEHAVIOUR
In all seek cases the same general principle applies: find the nearest checkpoint in the cache
with a frame number strictly less than the requested frame N, and compute forward from there
in ascending frame order. The cases below describe what to do when that search succeeds or fails.

Before any anchor search, always check whether the requested frame N is already present in
the cache. If it is, return it immediately without any recomputation.

Jump forward (requested frame far ahead of frontier):
    find nearest checkpoint in cache with frame number < requested frame
    if found: anchor from there, compute forward to requested frame
    if not found: treat as normal cache-miss, request prior cache_capacity
                  source frames, compute forward chain from furthest available
                  checkpoint or from frame 0 if none available
    recompute forward from there - cost is proportional to the distance from
    that checkpoint to the requested frame
    this is the worst case for use case (c) user visual viewing with large jumps

Jump backward (requested frame behind all cached frames):
    if a checkpoint exists in cache with frame number < requested frame
    and within prior cache_capacity: anchor from there
    if not found: same treatment as jump-forward not-found case above

Frame 0 checkpoint retained permanently assists both cases by always
providing a last-resort anchor.

# PRUNING LOCATION AND TRIGGERS

## Where pruning runs
Pruning runs only inside arAllFramesReady,
specifically after all hole-filling and output frame computation are complete and all
cache writes for that invocation are done,
but before releasing source frames and returning the output frame to VS.
This means:
    - under fmParallelRequests pruning is automatically serialised
        - only one arAllFramesReady runs at a time so pruning never runs
          concurrently with itself or with any cache write
    - under fmParallel the entire pruning pass must be a single mutex-protected
      critical section
Pruning never runs in arInitial
    - arInitial only reads the cache index to find the nearest checkpoint,
      it never modifies the cache.

## Triggers - what causes a pruning pass
Two independent triggers, both checked at the same point in arAllFramesReady:

Trigger 1 - non-checkpoint pool overflow
    if non_checkpoint_pool.size() > cache_capacity * overflow_factor:
        prune non-checkpoint pool
        evict lowest frame numbers first
        stop when non_checkpoint_pool.size() <= cache_capacity
    This is the normal steady-state trigger during linear playback.
    As the frontier advances the pool fills, overflows, and is trimmed back to nominal capacity.

Trigger 2 - checkpoint pool overflow
    if checkpoint_pool.size() > checkpoint_max_retain:
        prune checkpoint pool
        evict lowest frame numbers first
        subject to never-evict constraints:
            never reduce below checkpoint_min_retain
            never evict the two checkpoints nearest to frontier
            never evict frame 0 checkpoint
        stop when checkpoint_pool.size() <= checkpoint_max_retain
        or when never-evict constraints prevent further eviction
    This trigger fires less frequently than Trigger 1 - roughly once every
    checkpoint_interval frames during linear playback.

## Order of operations within arAllFramesReady including pruning
    1.  retrieve source frames via getFrameFilter() for the full range back to
        the most recent checkpoint and source frame N itself
    2.  {mutex} check cache for holes and identify nearest checkpoint strictly
        prior to frame N as the computation anchor {release mutex}
    3.  compute hole fills in ascending frame order using retrieved source frames
        and cached output frames as the chain anchor
    4.  {mutex} conditional store hole fills only if not already filled by another
        thread, update checkpoints {release mutex}
    5.  compute output frame N
    6.  {mutex} conditional store output frame N only if not already filled by
        another thread, update checkpoints {release mutex}
    7.  CHECK TRIGGER 1: {mutex} if non-checkpoint pool over overflow capacity,
        prune {release mutex}
    8.  CHECK TRIGGER 2: {mutex} if checkpoint pool over max_retain,
        prune {release mutex}
    9.  release all source frames retrieved via getFrameFilter()
    10. return output frame N to VS

Steps 7 and 8 are the pruning location.
They run after all writes for this invocation are committed, so pruning decisions
see a fully consistent cache state.
They run before source frame release and return so any error in pruning does not
affect the output frame already computed.

## One additional trigger - teardown
At cnr3_free() all remaining frames in both pools must be released regardless of size.
This is not pruning in the eviction sense but it uses the same release path.
Every slot in both pools calls freeFrame() and the pools are cleared.

## What does NOT trigger pruning
    - arInitial never triggers pruning
    - reading a frame from the cache as an anchor does not trigger pruning
    - a cache miss does not trigger pruning
    - exceeding nominal capacity alone does not trigger pruning - only exceeding
      overflow capacity triggers it, giving headroom for burst computation
      during seeks and hole-filling

# THREAD SAFETY SUMMARY

Under fmParallelRequests:
    - multiple arInitial calls may run concurrently - arInitial only reads
      the cache index and calls requestFrameFilter(), both safe under mutex
    - only one arAllFramesReady runs at a time - all cache writes, hole fills,
      checkpoint promotions, and pruning passes are automatically serialised
    - the per-instance mutex in Cnr3Data protects concurrent arInitial reads
      from the single concurrent arAllFramesReady write
    - no per-slot reference count needed
    - no condition variable needed

Under fmParallel (future):
    - both arInitial and arAllFramesReady may run concurrently in multiple threads
    - the per-instance mutex in Cnr3Data must protect all cache reads and writes
    - the entire pruning pass must be a single mutex-protected critical section
    - per-slot active_readers reference count must be implemented before
      fmParallel development begins to prevent eviction of in-use frames
    - condition variables per cache slot must be implemented to handle the case
      where one thread needs output[N-1] as an anchor while another thread is
      currently computing output[N-1]

Instance isolation (both modes):
    - each CNR3 filter instance owns exactly one Cnr3Data object
    - the per-instance mutex lives inside Cnr3Data and is never shared across instances
    - threads serving instance A and threads serving instance B never contend
      on the same mutex and never access each other's cache
    - g_cnr3_next_instance_id is the only global mutable state and is accessed
      only once per instance at creation time via an atomic operation

# POSSIBLE USE CASES
Pruning methods need to take into account use cases:
    (a) Normally
        - incremental walking up the frame numbers in ascending order
          (some will of course be out of order) [0]..[last_frame_number]
    (b) an upstream filter does temporal computations and requests a frame and some frames either side
        - usually in sequence from [0]..[last_frame_number], but
            - for a frame [N] it may request of VS perhaps 2 to 6 frames into the future
            - perhaps some frame action initially then probably to cnr3 it looks like
              case (a) since VS will cache CNR3 output frames
    (c) very small probability; a user visual viewing using the script
        - large jumps up the frame count, then incremental up
        - large jumps down the frame count, then incremental up
    (d) tiny probability
        - incremental walking down the frame numbers in descending order
          (some will of course be out of order) [last_frame_number]..[0]

# DEBUG CATEGORY REGISTRY
All debug output uses structured tags for grep-ability.
Format: CNR3 [inst=N] [frame=NNN] [thr=X] [TAG] message

Tags:
    CACHE_HIT        output frame found in cache, computation skipped
    CACHE_MISS       output frame not in cache, computation required
    CACHE_RACE       output frame computed but already filled by another thread, discarded
    CACHE_WRITE      output frame successfully written to cache
    CACHE_PRUNE      frame evicted from cache, with reason
    CACHE_OVERFLOW   cache exceeded nominal capacity, pruning triggered
    CACHE_CAPACITY   cache occupancy at a given moment (frame count and checkpoint count)
    CKPT_PROMOTE     output frame promoted to checkpoint
    CKPT_ANCHOR      checkpoint selected as computation anchor
    CKPT_EVICT       checkpoint frame evicted, next anchor is now frame X
    CKPT_COUNT       current number of checkpoints in cache
    HOLE_COUNT       number of holes identified in range at arAllFramesReady entry
    HOLE_PREFILLED   hole found filled on recheck, skipped
    OUT_RACE         requested output frame computed but already in cache, discarded
    OUT_WRITE        requested output frame written to cache
    SRC_REQUEST      source frame range requested in arInitial
    SRC_RETRIEVE     source frame range retrieved in arAllFramesReady
    SEEK_FORWARD     large forward jump detected, anchoring from checkpoint X
    SEEK_BACKWARD    large backward jump detected, anchoring from checkpoint X
    STARTUP_CHAIN    computing startup forward chain from frame 0
    MUTEX_WAIT       thread waited on mutex (fmParallel only)
    FRAME_DONE       output frame returned to VS
    TEARDOWN_RELEASE releasing N frames from cache at filter teardown
    SUMMARY          per-instance aggregate statistics at filter teardown

Per-instance summary printed at teardown includes:
    frames_processed
    total_holes_identified
    holes_prefilled_by_recheck
    holes_computed
    holes_discarded_race
    output_discarded_race
    checkpoint_anchors_used
    avg_anchor_distance
    cache_hit_rate
    wasted_computation_rate

**********************************************************************************************
END APPENDIX B - CACHE MANAGEMENT for CNR3
**********************************************************************************************