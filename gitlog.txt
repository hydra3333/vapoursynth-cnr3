958a16a  CMS07-H.1A: prove bounded recovery search scaffold
Introduce and prove the AS1 bounded recovery search scaffold.

Add Cnr3CacheRecoverySearchPlan and plan_bounded_recovery_search(). The
planner searches downward from requested_frame - 1 within the bounded recovery
window only, using an inclusive lower bound of requested_frame minus the
configured recovery back radius. The recovery back radius is statically tied to
the CMS hot-zone back radius so the B=50 recovery window cannot silently drift
from the design.

Prove nearest-present-wins recovery anchor selection. The checkpoint flag is
irrelevant to anchor eligibility: a nearer non-checkpoint beats a farther
checkpoint, and a nearer checkpoint beats a farther non-checkpoint. The search
does not consider the requested frame itself as an anchor, does not walk below
the bounded lower edge, and clamps safely at frame 0 without unsigned underflow.

Add recovery hole-catalogue planning. Once the nearest anchor is selected, the
hole catalogue is the contiguous missing range from anchor + 1 through
requested_frame - 1. The requested frame is recorded separately as the later
repair target, not as a hole-catalogue entry. This follows from
nearest-present-wins semantics: if any frame between anchor and requested were
present, that frame would be the anchor.

Add H.1A selftest coverage. The proof pins both search-window edges, including
candidate-at-requested-minus-B found and candidate-at-requested-minus-B-minus-1
excluded. It verifies requested-frame exclusion, bounded-stop behaviour,
checkpoint-flag irrelevance in both directions, contiguous hole-catalogue
construction, requested-frame repair-target accounting, and lower-bound
underflow clamp behaviour. Add verbose trace output for the human-followable
recovery search scenarios.

Keep this as a read-only recovery search scaffold proof only. Do not pin
anchors, record recovery pins, call AS2, store frames, request or retrieve
source frames, recompute, return frames, prune, change D-SUM diagnostics,
connect to getFrame production flow, touch source lifecycle, or touch pixel
behaviour.

Verified:

* Plugin Debug|x64 build succeeds.
* Plugin Release|x64 build succeeds.
* Selftest console Debug|x64 build succeeds.
* Selftest console Release|x64 build succeeds.
* Debug x64 selftest execution reports 28/28 PASS and returns 0.
* Release x64 selftest execution reports 28/28 PASS and returns 0.
* Release x64 forced-failure harness reports 27/28 PASS, 1 FAIL, and returns 1.
* Release x64 verbose selftest execution reports 28/28 PASS and returns 0.


353bad6  CMS07-G.13A: wire D-SUM-11 prune rejection counter
Wire the deferred D-SUM-11 hot-zone prune-rejection counter without changing
prune behaviour.

Add observation-only counting for
frames_rejected_from_prune_due_to_hot_zone during a real AS5 prune pass. The
counter is updated only after the prune trigger fires, using the same locked
cache snapshot as prune selection. It counts genuinely cached, unpinned,
otherwise-prunable frames that are rejected from prune selection because they
are inside an active hot zone. The update is guarded by
CNR3_DIAG_COMPUTE_DSUM11_HOT_ZONE and uses allocation-free saturating diagnostic
counter addition. The cache core remains silent and no D-SUM formatting or
printing is added.

Extend the AS5 selftest with a discriminating D-SUM-11 counter scenario. The
test pins frames inside the active hot zone so the real rejected-frame count is
58, while the hot-zone width is 61. This proves the counter tracks actual cached
and prunable state rather than merely returning the zone width. The test also
checks that the no-prune boundary leaves the counter unchanged and that an
idempotent post-prune pass does not increment the counter again.

Satisfy R-PROCESS-19 for the D-SUM compute-gate change. The compute-enabled
test run proves the counter path, and the compute-disabled run with
CNR3_DIAG_COMPUTE_DSUM11_HOT_ZONE undefined proves that the D-SUM observation
path compiles out cleanly while non-D-SUM cache-core behaviour remains
unchanged.

Keep this as a diagnostic counter-wiring proof only. Do not change prune
selection, detach/free behaviour, D-SUM printing, recovery planning,
VapourSynth getFrame flow, source lifecycle, or pixel behaviour.

Verified:

* Plugin Debug|x64 build succeeds.
* Plugin Release|x64 build succeeds.
* Selftest console Debug|x64 build succeeds.
* Selftest console Release|x64 build succeeds.
* Compute-enabled Debug x64 selftest execution reports 27/27 PASS and returns 0.
* Compute-enabled Release x64 selftest execution reports 27/27 PASS and returns 0.
* Compute-enabled Release x64 forced-failure harness reports 26/27 PASS, 1 FAIL, and returns 1.
* Compute-enabled Release x64 verbose selftest execution reports 27/27 PASS and returns 0.
* Compute-disabled Release x64 selftest execution reports 27/27 PASS and returns 0.
* Compute-disabled Release x64 forced-failure harness reports 26/27 PASS, 1 FAIL, and returns 1.


5f479be  CMS07-G.12A: prove AS2 store-record checkpoint atomic
Introduce and prove the AS2 combined store-record monotonic checkpoint atomic.

Add store_owned_frame_and_record_pin() as the combined AS2 primitive for one
computed output frame. The helper reserves per-invocation pin-list capacity
before taking the cache lock, then performs store, monotonic checkpoint flag
handling, pin, and pin-record under one lock acquisition. It preserves
first-in-best-dressed frame data, promotes an existing non-checkpoint slot when
a checkpoint-eligible duplicate arrives, never demotes an existing checkpoint
when a non-checkpoint duplicate arrives, and records exactly one pin for every
AS2 call, including duplicate-store calls.

Document the two load-bearing lifetime and ownership details in the AS2 code:
the public helper takes the incoming frame by value so a rejected duplicate
loser remains outside the nested lock scope and releases after cache_mutex_ has
unlocked, and duplicate-store calls pin and record the existing winner slot
because the caller's contract is store-and-pin frame N when frame N is present.

Add AS2 store-record monotonic checkpoint selftest coverage. The proof checks
fresh non-checkpoint store, fresh checkpoint store, checkpoint duplicate
promotion, non-checkpoint duplicate non-demotion, first-in-best-dressed data
retention, duplicate loser release count, winner lookup authority, recorded pin
counts, duplicate-store pin discharge, and final clear release counts. Add
verbose selftest trace output for the human-followable AS2 scenario.

Keep this as an AS2 cache-core atomic proof only. Do not wire D-SUM-11
prune-rejection counter increments, perform recovery planning, connect to
VapourSynth getFrame / AS1 / AS2 production flow, request or retrieve source
frames, touch source lifecycle, touch pixel behaviour, or remove the earlier
isolated store proof helpers.

Verified:

* Plugin Debug|x64 build succeeds.
* Plugin Release|x64 build succeeds.
* Selftest console Debug|x64 build succeeds.
* Selftest console Release|x64 build succeeds.
* Debug x64 selftest execution reports 27/27 PASS and returns 0.
* Release x64 selftest execution reports 27/27 PASS and returns 0.
* Release x64 forced-failure harness reports 26/27 PASS, 1 FAIL, and returns 1.
* Release x64 verbose selftest execution reports 27/27 PASS and returns 0.


c2c669c  CMS07-G.11A: prove AS5 prune execution
Introduce and prove AS5 prune execution by composing the previously proven
prune trigger, composite candidate selection, bounded detach, and central
remove helpers.

Add Cnr3CachePruneExecutionSummary and execute_bounded_prune_pass(). The public
helper performs the AS5 lock-boundary shape: decide, select, and detach under
one cache lock, then release detached frame references after the lock has been
released. The locked helper checks cache invariants before and after detach,
uses the G.10A trigger decision, bounds selection by the requested remove count,
uses the G.9A composite selector, and detaches only the selected slots.

Add AS5 prune execution selftest coverage. The proof exercises both sides of
the trigger boundary with real stores, verifies prune-to-ceiling execution
through a bounded remove limit, proves pinned-frame exclusion inside a cold
candidate band, proves checkpoint retention keeps frame 0 while allowing a
colder checkpoint to be removed, verifies exact frame release counts for
removed and surviving frames, verifies survivor release only at teardown, and
verifies an idempotent second prune pass detaches and frees nothing after the
cache has fallen below the trigger.

Add verbose selftest trace output for the human-followable AS5 scenario. The
trace is test infrastructure only; the cache core remains silent and does not
gain diagnostic-dependent behaviour.

Keep this as an AS5 prune execution proof only. Do not wire D-SUM-11
prune-rejection counter increments, perform recovery planning, connect to
VapourSynth getFrame / AS1 / AS5 production flow, request or retrieve source
frames, touch source lifecycle, or touch pixel behaviour.

Verified:

* Plugin Debug|x64 build succeeds.
* Plugin Release|x64 build succeeds.
* Selftest console Debug|x64 build succeeds.
* Selftest console Release|x64 build succeeds.
* Debug x64 selftest execution reports 26/26 PASS and returns 0.
* Release x64 selftest execution reports 26/26 PASS and returns 0.
* Release x64 forced-failure harness reports 25/26 PASS, 1 FAIL, and returns 1.
* Release x64 verbose selftest execution reports 26/26 PASS and returns 0.


3f99a82  CMS07-G.10A: prove prune trigger hysteresis decision
Introduce and prove the prune trigger decision arithmetic without performing
prune, selection, detach, or frame release.

Add Cnr3CachePruneTriggerDecision and
cnr3_calculate_cache_prune_trigger_decision(). The decision function computes
the active cache ceiling from byte budget and frame size, clamps it to the
configured minimum and maximum, derives the overflow trigger from the clamped
ceiling using the explicit 11/10 overflow factor, and fires only when the
current slot count is strictly greater than the trigger. When pruning is
required, the target is the active ceiling, not the trigger threshold and not an
empty cache.

Add prune trigger hysteresis selftest coverage. The proof pins both sides of
the trigger boundary for maximum-ceiling, mid-ceiling, and minimum-ceiling
cases; verifies prune-to-ceiling removal counts; verifies no-prune boundary
behaviour; verifies all decision fields; and verifies reset-on-error behaviour
for invalid frame byte counts. Add verbose selftest trace output for the
human-followable trigger scenarios.

Keep this as a pure prune trigger decision proof only. Do not select victims,
detach slots, release frames, call the composite selector, implement final AS5
prune decide/detach/free, wire D-SUM-11 prune-rejection counter increments,
perform recovery planning, connect to getFrame / AS1 / AS5 production flow,
touch source lifecycle, or touch pixel behaviour.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 25/25 PASS and returns 0.
- Release x64 selftest execution reports 25/25 PASS and returns 0.
- Release x64 forced-failure harness reports 24/25 PASS, 1 FAIL, and returns 1.
- Release x64 verbose selftest execution reports 25/25 PASS and returns 0.


946fde5  CMS07-G.9A: prove composite prune candidate selection
Introduce and prove composite prune candidate selection without performing
prune or detach.

Add select_composite_prune_candidates_bounded(), which assembles the previously
proven candidate-selection clauses into a selection-only helper. It selects
unpinned slots outside every active hot zone, includes checkpoint candidates
only when checkpoint retention permits, includes non-checkpoint candidates only
when externally supplied capacity permission permits, and orders the selected
candidates by greatest distance from the nearest active hot-zone boundary.

Add composite prune candidate selection selftest coverage. The proof checks
pinned-slot exclusion, hot-zone exclusion, frame-0 checkpoint exclusion,
checkpoint retention limiting, coldest-checkpoint-first selection,
non-checkpoint capacity permission, capacity-disabled checkpoint-only mode,
checkpoint-retention-disabled non-checkpoint-only mode, mixed
checkpoint/non-checkpoint nearest-zone distance ordering, bounded top-K
selection, and cache-state invariant cleanliness. The selector does not detach,
prune, free frames, or change cache contents.

Add a cross-pool bounded top-K scenario proving that the merged candidate list
is the globally correct coldest prefix across checkpoint and non-checkpoint
pools. The test asserts that a checkpoint can correctly displace a locally
eligible non-checkpoint, and that a non-checkpoint can correctly displace a
budget-eligible lower-ranked checkpoint. The verbose trace reports the scenario,
the returned global top-K, and the asserted absent candidates for human review.

Keep this as a composite prune candidate selection proof only. Do not implement
the CMS07 section 7.2 active-ceiling / overflow-factor trigger, prune toward
active_ceiling, detach slots, release frames, implement final AS5 prune
decide/detach/free, wire D-SUM-11 prune-rejection counter increments, perform
recovery planning, connect to getFrame / AS1 / AS5 production flow, touch
source lifecycle, or touch pixel behaviour.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 24/24 PASS and returns 0.
- Release x64 selftest execution reports 24/24 PASS and returns 0.
- Release x64 forced-failure harness reports 23/24 PASS, 1 FAIL, and returns 1.
- Release x64 verbose selftest execution reports 24/24 PASS and returns 0.


32bf9d0  CMS07-G.9A: prove composite prune candidate selection
Introduce and prove composite prune candidate selection without performing
prune or detach.

Add select_composite_prune_candidates_bounded(), which assembles the previously
proven candidate-selection clauses into a selection-only helper. It selects
unpinned slots outside every active hot zone, includes checkpoint candidates
only when checkpoint retention permits, includes non-checkpoint candidates only
when externally supplied capacity permission permits, and orders the selected
candidates by greatest distance from the nearest active hot-zone boundary.

Add composite prune candidate selection selftest coverage. The proof checks
pinned-slot exclusion, hot-zone exclusion, frame-0 checkpoint exclusion,
checkpoint retention limiting, coldest-checkpoint-first selection,
non-checkpoint capacity permission, capacity-disabled checkpoint-only mode,
checkpoint-retention-disabled non-checkpoint-only mode, mixed
checkpoint/non-checkpoint nearest-zone distance ordering, bounded top-K
selection, and cache-state invariant cleanliness. The selector does not detach,
prune, free frames, or change cache contents.

Keep this as a composite prune candidate selection proof only. Do not implement
the CMS07 section 7.2 active-ceiling / overflow-factor trigger, prune toward
active_ceiling, detach slots, release frames, implement final AS5 prune
decide/detach/free, wire D-SUM-11 prune-rejection counter increments, perform
recovery planning, connect to getFrame / AS1 / AS5 production flow, touch
source lifecycle, or touch pixel behaviour.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 24/24 PASS and returns 0.
- Release x64 selftest execution reports 24/24 PASS and returns 0.
- Release x64 forced-failure harness reports 23/24 PASS, 1 FAIL, and returns 1.
- Release x64 verbose selftest execution reports 24/24 PASS and returns 0.


badac7c  CMS07-G.8A: prove prune-victim distance ordering
Introduce and prove ordering among already-eligible prune candidates.

Add a selector that orders prune candidates by greatest distance from the nearest
active hot-zone boundary. This uses the minimum distance across all active zones
for each candidate, so a frame adjacent to one zone is treated as near activity
even if it is far from another zone. The selector returns a bounded ordered list
and does not detach slots, release frames, prune, or change cache contents.

Add prune-victim distance-ordering selftest coverage. The proof exercises a
multi-zone scenario and verifies that genuinely isolated frames rank ahead of
frames adjacent to any active zone. It also verifies bounded top-K selection and
that cache-state invariants remain clean.

Add a selftest-only --verbose option that prints the G.8A scenario, active
hot-zone ranges, eligible cached frames, returned order, nearest-zone distances,
and expected order through the existing diagnostic output path. This is test
infrastructure only; the cache core remains silent and has no diagnostic-
dependent behaviour.

Keep this as a prune-victim ordering proof only. Do not assemble the full CMS07
section 7.1 composite eviction predicate, implement the CMS07 section 7.2
active-ceiling / overflow-factor prune trigger, prune toward active_ceiling,
implement final AS5 prune policy, wire D-SUM-11 prune-rejection counter
increments, perform recovery planning, connect to getFrame / AS1 / AS5
production flow, touch source lifecycle, or touch pixel behaviour.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 23/23 PASS and returns 0.
- Release x64 selftest execution reports 23/23 PASS and returns 0.
- Release x64 forced-failure harness reports 22/23 PASS, 1 FAIL, and returns 1.
- Release x64 verbose selftest execution reports 23/23 PASS and returns 0.


d96acb6  REVISED CMS07.2 and Associated companion doc


2b7d933  CMS07-G.7A: prove hot-zone prune protection selection
Introduce and prove hot-zone-protected prune candidate selection.

Add a bounded selector/detach helper that removes only cached slots that are
unpinned, non-checkpoint, and outside every active hot zone. This proves the
hot-zone exclusion clause for future prune selection while preserving the
existing bounded detach and post-lock frame-release discipline.

Add hot-zone prune-protection selection selftest coverage. The proof checks
that active hot-zone ranges protect otherwise removable non-checkpoint slots,
that unprotected non-checkpoint slots remain eligible, that pinned slots are
still excluded, that checkpoint slots are still excluded, that bounded removal
limits are respected, and that cache-state invariants remain clean.

Keep this as a hot-zone prune-protection selection proof only. Do not assemble
the full CMS07 section 7.1 composite eviction predicate, implement
greatest-hot-zone-distance victim ordering, implement the CMS07 section 7.2
active-ceiling / overflow-factor prune trigger, evaluate checkpoint-retention
limits as part of this helper, wire D-SUM-11 prune rejection counter increments,
implement final prune policy, perform recovery planning, connect to getFrame /
AS1 / AS5 production flow, touch source lifecycle, or touch pixel behaviour.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 22/22 PASS and returns 0.
- Release x64 selftest execution reports 22/22 PASS and returns 0.
- Release x64 forced-failure harness reports 21/22 PASS, 1 FAIL, and returns 1.


0fbc1f4  updated specs


c77882b  CMS07-G.6A: prove D-SUM-11 hot-zone counters
Introduce and prove the D-SUM-11 hot-zone diagnostic counter model.

Add cache-specific D-SUM-11 hot-zone diagnostic stats and observation helpers
under the cache diagnostics module. Track hot-zone create, slide, merge, decay,
expiry, zone-count samples, protected-range samples, and the deferred prune
rejection counter field. Keep the cache core as the owner of the per-cache
stats instance and lock-held observation points, without adding formatting,
stderr emission, allocation, or behaviour decisions.

Add D-SUM-11 hot-zone counter model selftest coverage. The proof checks create,
slide, merge, decay, expiry, active-zone sample accounting, protected-range
sample accounting, deferred prune-rejection field state, and macro-off no-op
behaviour for the D-SUM-specific test.

Keep this as a D-SUM-11 counter-model proof only. Do not implement the
human-readable D-SUM-11 printer, cnr3_diag_write_line() output, table
formatting, stderr emission, pruning, prune rejection counter bumps,
AS1/getFrame wiring, source lifecycle handling, pixel behaviour, or D-SUM
printer integration.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 enabled selftest execution reports 21/21 PASS and returns 0.
- Release x64 enabled selftest execution reports 21/21 PASS and returns 0.
- Release x64 enabled forced-failure harness reports 20/21 PASS, 1 FAIL, and returns 1.
- Release x64 compute-disabled selftest execution reports 21/21 PASS and returns 0.
- Release x64 compute-disabled forced-failure harness reports 20/21 PASS, 1 FAIL, and returns 1.
- The D-SUM-11 compute-disabled build proves the counter path observes only and
  does not change the existing cache-core behaviour covered by the other
  selftests.


3dbfc44  updated test .vpp files using bubling and jumping


e19211d  CMS07-G.5A: prove hot-zone retirement decay
Introduce and prove isolated hot-zone retirement by decay margin.

Add retire_decay_eligible_hot_zones() and supporting locked helpers. A hot zone
is retired only when the CMS07.1 decay margin has elapsed since its last
observed frame and no currently pinned cache slot lies inside that hot-zone
range. Retirement removes only hot-zone protection; it does not remove cache
slots, alter checkpoint state, or perform pruning.

Add hot-zone retirement decay lifecycle selftest coverage. The proof checks
invalid retirement frame rejection, no retirement before the decay margin,
retirement once the decay margin elapses, no retirement when the current frame
is below last_observed_frame, pinned-slot protection inside the zone, retirement
after pin discharge, checkpoint independence, checkpoint slot preservation, and
cache-state invariant cleanliness.

Keep this as a hot-zone retirement/decay proof only. Do not implement
prune-policy use, cache-slot pruning from hot-zone retirement, distance
ordering, active-ceiling policy, recovery planning, AS1/getFrame wiring, source
lifecycle handling, pixel behaviour, D-SUM production counters, human-readable
hot-zone instrumentation, or scaffold gates.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 20/20 PASS and returns 0.
- Release x64 selftest execution reports 20/20 PASS and returns 0.
- Release x64 forced-failure harness reports 19/20 PASS, 1 FAIL, and returns 1.


5de0d38  CMS07-G.4A: prove hot-zone capacity merge
Introduce and prove the conservative full-capacity hot-zone merge path.

When a distant observation cannot slide any active zone and all hot-zone slots
are active, merge the two closest active zones conservatively, then spawn the
new observation zone into the freed slot. The merged zone preserves coverage
from the minimum low frame to the maximum high frame of the merged pair, and the
new distant observation receives its own zone.

Add hot-zone capacity merge lifecycle selftest coverage. The proof checks that
full capacity no longer fails merely because the zone list is full, the closest
pair is merged, the merged gap becomes covered, the new distant observation is
spawned, the hot-zone count remains bounded, unmerged zones remain active,
normal near-observation slide behaviour still works after merge, clear()
removes hot-zone state, and cache-state invariants remain clean.

Keep this as a hot-zone capacity merge proof only. Do not implement hot-zone
retirement, decay transition, prune-policy use, distance ordering,
active-ceiling policy, recovery planning, AS2 store/adopt/pin/record/checkpoint
behaviour, D-SUM production counters, getFrame wiring, source lifecycle
handling, pixel behaviour, human-readable instrumentation, or scaffold gates.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 19/19 PASS and returns 0.
- Release x64 selftest execution reports 19/19 PASS and returns 0.
- Release x64 forced-failure harness reports 18/19 PASS, 1 FAIL, and returns 1.


c81a0f6  CMS07-G.3A: prove hot-zone slide and spawn
Introduce and prove the first hot-zone observation update behaviour.

Add record_hot_zone_observation() and frame_is_inside_hot_zone() helpers with
locked implementations. The observation helper rejects invalid frame numbers,
spawns the first active zone, slides the nearest active zone within the
CMS07.1 jump threshold, spawns a new zone for distant observations while
capacity permits, and rejects distant observations cleanly when the hot-zone
capacity is full.

Add hot-zone slide/spawn lifecycle selftest coverage. The proof checks first
spawn, window bounds, near-observation slide, replace-not-extend slide
behaviour, distant spawn, nearest-zone slide selection, max-zone capacity
enforcement, clear() cleanup, and cache-state invariant integration.

Keep this as a hot-zone slide/spawn proof only. Do not implement hot-zone merge,
retirement, decay transition, prune-policy use, distance ordering,
active-ceiling policy, recovery planning, AS2 store/adopt/pin/record/checkpoint
behaviour, D-SUM production counters, getFrame wiring, source lifecycle
handling, pixel behaviour, human-readable instrumentation, or scaffold gates.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 18/18 PASS and returns 0.
- Release x64 selftest execution reports 18/18 PASS and returns 0.
- Release x64 forced-failure harness reports 17/18 PASS, 1 FAIL, and returns 1.


3b54873  CMS07-G.2A: add hot-zone data model
Introduce and prove the hot-zone data model skeleton for CMS07.

Add Cnr3CacheHotZone, hot-zone validity helpers, hot-zone invariant checks,
hot-zone storage in Cnr3OutputCacheCore, and a lock-owning hot_zone_count()
observer. Include hot-zone state in empty(), clear(), and cache-state invariant
validation.

Add hot-zone data model selftest coverage. The proof checks default inactive
zone validity, active zone validity, invalid stale inactive zones, invalid
active ranges, invalid last-observed positions, default cache hot-zone state,
clear() hot-zone cleanup, and cache-level invariant integration.

Keep this as a data-model proof only. Do not implement hot-zone slide, spawn,
merge, retirement, decay transition, prune-policy use, distance ordering,
active-ceiling policy, recovery planning, AS2 store/adopt/pin/record/checkpoint
behaviour, D-SUM production counters, getFrame wiring, source lifecycle
handling, pixel behaviour, or scaffold gates.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 17/17 PASS and returns 0.
- Release x64 selftest execution reports 17/17 PASS and returns 0.
- Release x64 forced-failure harness reports 16/17 PASS, 1 FAIL, and returns 1.


dd96ec3  CMS07-G.1A: add cache policy constants
Introduce the CMS07.1 cache policy constants and coherence checks.

Add the cache byte budget, active-ceiling min/max frame bounds, overflow ratio,
checkpoint interval and retention constants, hot-zone radii, bounded recovery
back radius, max hot-zone count, derived jump threshold, hot-zone decay margin,
and bounded prune victim limit.

Codify the CMS07.1 CR1-CR5 coherence rules and the decay-margin bound as
comments directly above the relevant constants, with compile-time static_assert
checks for the relationships currently expressible at constant-definition time.

Add cache-policy constants selftest coverage. The proof checks the CMS07.1
numeric values, derived jump threshold, bounded recovery / hot-zone radius
relationship, checkpoint retain ordering, active-ceiling ordering, CR4 headroom
relationship, CR5 checkpoint retain floor, decay-margin bound, and overflow
ratio.

Keep this as a constants and coherence proof only. Do not implement active-ceiling
calculation, hot-zone state, prune candidate ordering, final prune policy,
recovery planning, AS2 store/adopt/pin/record/checkpoint behaviour, D-SUM
production counters, getFrame wiring, source lifecycle handling, pixel
behaviour, or scaffold gates.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 16/16 PASS and returns 0.
- Release x64 selftest execution reports 16/16 PASS and returns 0.
- Release x64 forced-failure harness reports 15/16 PASS, 1 FAIL, and returns 1.


5b901aa  CMS07-F.4A: prove checkpoint retention boundary
Introduce and prove the checkpoint retention boundary helper for the future
bounded prune path.

Add remove_unpinned_checkpoints_above_retain_count_bounded() and its locked
helper. The helper selects and detaches at most the requested bound in one
lock-owning operation, never selects frame 0, never selects pinned checkpoints,
never selects non-checkpoints, and does not reduce checkpoint_count below the
caller-provided retain floor. Detached frame ownership is released only after
the cache lock exits.

Add checkpoint retention boundary lifecycle selftest coverage. The proof checks
bounded checkpoint removal, retain-floor preservation, frame-0 preservation,
pinned-checkpoint exclusion, non-checkpoint exclusion, post-unpin checkpoint
eligibility, checkpoint-index consistency, frame-index consistency, moved-slot
lookup after compaction, and post-lock frame release.

Keep this as a checkpoint retention boundary proof only. Do not implement final
checkpoint prune policy, hot-zone exclusion, greatest-distance ordering,
active-ceiling pressure, recovery state, AS2 store/adopt/pin/record/checkpoint
behaviour, D-SUM production counters, getFrame wiring, source lifecycle
handling, pixel behaviour, or scaffold gates.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 15/15 PASS and returns 0.
- Release x64 selftest execution reports 15/15 PASS and returns 0.
- Release x64 forced-failure harness reports 14/15 PASS, 1 FAIL, and returns 1.


19f0c81  CMS07-F.3A: prove unpinned non-checkpoint selection
Introduce and prove the narrow unpinned non-checkpoint selection helper for the
future bounded prune path.

Add remove_unpinned_noncheckpoint_frames_bounded() and its locked helper. The
helper selects and detaches at most the requested bound in one lock-owning
operation, selects only slots with pin_count == 0 and is_checkpoint == false,
preserves pinned slots, preserves checkpoint slots, and releases detached frame
ownership only after the cache lock exits.

Add unpinned non-checkpoint selection lifecycle selftest coverage. The proof
checks bounded selection, pinned-slot exclusion, checkpoint exclusion,
post-unpin eligibility, checkpoint ineligibility after unpin, frame-index and
checkpoint-index consistency, moved-slot lookup after compaction, and post-lock
frame release.

Keep this as a narrow selection proof only. Do not implement final prune
candidate ordering, hot-zone policy, active-ceiling policy, checkpoint-retention
pruning, recovery, AS2 store/adopt/pin/record/checkpoint behaviour, D-SUM
production counters, getFrame wiring, source lifecycle handling, pixel
behaviour, or scaffold gates.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 14/14 PASS and returns 0.
- Release x64 selftest execution reports 14/14 PASS and returns 0.
- Release x64 forced-failure harness reports 13/14 PASS, 1 FAIL, and returns 1.


c36c641  CMS07-F.2A: prove bounded selected detach
Introduce and prove the bounded selected-detach helper for the future AS5 prune
path.

Add remove_selected_unpinned_frames_bounded() and its locked helper. The helper
accepts already-selected candidate frame numbers, detaches at most the requested
bound in one lock-owning operation, refuses pinned candidates, preserves
frame-index and checkpoint-index consistency, and releases detached frame
ownership only after the cache lock exits.

Add bounded-selected-detach lifecycle selftest coverage. The proof checks the
detach bound, survivor preservation beyond the bound, pinned-candidate rejection,
post-unpin removal, checkpoint index maintenance, moved-slot lookup after vector
compaction, missing/invalid candidate handling, and post-lock frame release.

Keep this as a bounded detach proof only. Do not implement final prune candidate
selection, hot-zone policy, active-ceiling policy, checkpoint-retention pruning,
recovery, AS2 store/adopt/pin/record/checkpoint behaviour, D-SUM production
counters, getFrame wiring, source lifecycle handling, pixel behaviour, or
scaffold gates.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 13/13 PASS and returns 0.
- Release x64 selftest execution reports 13/13 PASS and returns 0.
- Release x64 forced-failure harness reports 12/13 PASS, 1 FAIL, and returns 1.


9e222e3  CMS07-F.1A: prove central remove helper
Introduce and prove the central unpinned-frame remove helper needed before
bounded prune work.

Add remove_unpinned_frame() and the locked remove helper. The helper rejects
invalid frame numbers and missing frames, refuses to remove pinned slots,
detaches unpinned slots from the frame index and checkpoint index under the
cache lock, preserves vector/index consistency after compaction, and lets
detached frame ownership release after the cache lock exits.

Strengthen cache-state invariant checks for checkpoint slot-position validity,
duplicate checkpoint positions, and checkpoint-index completeness.

Add central-remove lifecycle selftest coverage. The proof checks invalid and
missing remove behaviour, pinned-slot rejection, non-checkpoint removal,
checkpoint removal, checkpoint index updates, moved-slot lookup after compaction,
post-lock frame release, and clean final teardown.

Keep this as a remove-helper proof only. Do not implement bounded prune policy,
hot-zone policy, recovery, AS2 store/adopt/pin/record/checkpoint behaviour,
D-SUM production counters, getFrame wiring, source lifecycle handling, pixel
behaviour, or scaffold gates.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 12/12 PASS and returns 0.
- Release x64 selftest execution reports 12/12 PASS and returns 0.
- Release x64 forced-failure harness reports 11/12 PASS, 1 FAIL, and returns 1.


5007489  Added CMS07.1.  CMS07-E.3A: prove monotonic checkpoint flag stores
Correct the CMS07-E.3A checkpoint duplicate-store policy for the CMS07.1
monotonic checkpoint flag rule.

Preserve first-in-best-dressed for frame data: the first stored output pixels
remain authoritative and duplicate stores do not overwrite cached frame data.

Allow checkpoint-eligible duplicate stores to promote an existing
non-checkpoint slot by raising its checkpoint flag and recording it in the
checkpoint index inside the same locked store operation. Preserve the
never-demote rule: non-checkpoint duplicate stores never clear checkpoint
classification or remove checkpoint retention.

Update the checkpoint-store lifecycle selftest so it fails if a
checkpoint-eligible duplicate leaves the slot non-checkpoint, overwrites the
existing frame data, leaks or double-frees the duplicate loser, demotes an
existing checkpoint, treats checkpoint status as a pin, or leaves inconsistent
checkpoint index state.

Keep this as an isolated store primitive correction only. Do not implement the
future AS2 store/adopt/pin/record/checkpoint helper, D-SUM production counters,
prune, recovery, getFrame wiring, source lifecycle handling, pixel behaviour,
or scaffold gates.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 11/11 PASS and returns 0.
- Release x64 selftest execution reports 11/11 PASS and returns 0.
- Release x64 forced-failure harness reports 10/11 PASS, 1 FAIL, and returns 1.


570331a  CMS07-E.3A: add checkpoint store flag proof
Add the isolated checkpoint-store primitive and proof for CMS07-E.3A.

Introduce store_checkpoint_owned_frame() alongside the existing non-checkpoint
store primitive. Refactor the locked store implementation so checkpoint and
non-checkpoint stores share the same first-in-best-dressed insertion path while
setting the slot checkpoint flag and checkpoint index inside the same
cache-lock operation.

Preserve duplicate-store behaviour: a duplicate checkpoint store must not
promote an existing non-checkpoint slot, and a duplicate non-checkpoint store
must not demote an existing checkpoint slot. Rejected duplicate frames remain
owned by the caller-side wrapper and are released after the cache lock exits.

Add checkpoint-store lifecycle selftest coverage. The new proof verifies
checkpoint_count(), cache invariants, duplicate loser release, no checkpoint as
active pin regression, clear/teardown behaviour, and first-in-best-dressed
classification preservation across duplicate checkpoint/non-checkpoint stores.

Keep this as an isolated store primitive only. Do not implement the future AS2
store/adopt/pin/record/checkpoint helper, D-SUM production counters, prune,
recovery, getFrame wiring, source lifecycle handling, pixel behaviour, or
scaffold gates.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 11/11 PASS and returns 0.
- Release x64 selftest execution reports 11/11 PASS and returns 0.
- Release x64 forced-failure harness reports 10/11 PASS, 1 FAIL, and returns 1.


f29f117  CMS07-E.2A: reconcile lookup-pin-record helper
Reconcile the original CMS07-E.2 lookup-pin-record helper obligation with the
AS1 helper implemented earlier in CMS07-D.3A.

Confirm lookup_frame_and_record_pin() is the single AS1 lookup-pin-record
primitive. It pre-reserves pin-list capacity before taking the cache lock, then
performs lookup, slot validation, pin-count increment, and no-allocation
pin-list record inside one cache-lock acquisition.

Document that no second public lookup-pin-record helper is required and that
the D.3A structural selftest remains the E.2A proof. Preserve the compile-time
checks that prevent reintroducing public gapped pin APIs or exposing a
caller-owned transient pin token.

Keep the future AS2 store/adopt/pin/record/checkpoint helper deferred until its
dependencies exist. Do not add APIs, selftests, D-SUM production counters,
checkpoint creation, prune, recovery, getFrame wiring, source lifecycle
handling, pixel behaviour, or scaffold gates.

Verified:
- Plugin Debug|x64 build succeeds.
- Plugin Release|x64 build succeeds.
- Selftest console Debug|x64 build succeeds.
- Selftest console Release|x64 build succeeds.
- Debug x64 selftest execution reports 10/10 PASS and returns 0.
- Release x64 selftest execution reports 10/10 PASS and returns 0.
- Release x64 forced-failure harness reports 9/10 PASS, 1 FAIL, and returns 1.

