Let me understand the anchor-selection and the prune/checkpoint-survival mechanism precisely — this is the heart of what P.11C.5 proves (a scene-cut checkpoint survives prune and is then found as the recovery anchor).I have enough architectural understanding, but rather than keep digging through cache-core internals, let me step back and recall what the D-series already proved about checkpoint survival and anchor selection — because P.11C.5's job is specifically the *new* end-to-end link, and I shouldn't re-prove what D.5 and the D-arc already established.That's the key architectural fact I needed, recalled precisely: **the recovery search selects the first-present output descending, regardless of checkpoint flag** — checkpoints aren't *preferred*, they just *survive eviction*, so in a cold region the surviving present frames *are* the checkpoints. So the end-to-end story P.11C.5 must prove is:

1. A scene cut creates a checkpoint (proven in .4).
2. That checkpoint **survives prune** when ordinary outputs around it are evicted (proven generically by D.5).
3. A later recovery search descending into that now-cold region **finds the scene-cut checkpoint as its anchor** (because it's the surviving present frame) — the exact-anchor recovery branch, anchored on a *scene-cut-created* checkpoint.

This is the genuinely new link — and I now understand it well enough to assess scope. Let me check what mechanism is already proven (D.1/D.2 exact-anchor recovery, D.5 prune survival) versus what's new, to determine whether .5 needs any source change or is proof-only.This settles the scope of P.11C.5 cleanly. Two decisive facts:

1. **`is_checkpoint` is a single bool with no provenance** — a scene-cut checkpoint and a grid checkpoint are *byte-identical* to the cache slot and the recovery search. The search descends and takes the first present frame regardless of *why* it's a checkpoint.

2. **Exact-anchor recovery onto a checkpoint is already a proven, live branch** (D.1/D.2, and the D3 harness even has a `d2regress` exact-anchor mode).

So P.11C.5's "new" link — *a scene-cut-created checkpoint being found as a recovery anchor* — is, mechanically, the **composition of two already-proven things**: P.11C.4 (a scene cut creates a checkpoint) + D.1/D.2/D.5 (a checkpoint survives prune and is found as an exact anchor). The code path doesn't distinguish the checkpoint's origin, so **there is essentially no new code to write or prove at the unit level** — it's an *integration/end-to-end* proof that the composition behaves as intended.

That's an important finding, and it means I should be careful not to over-engineer .5. Let me write the approach analysis capturing this — it'll likely be proof-only (a `.vpy` scenario), possibly even "already covered by composition" with a confirmatory harness.P.11C.5 approach analysis done and staged — `CNR3_P11C_5_Approach_Analysis_v1_0.txt`. The key finding shapes everything: **P.11C.5 is a composition of already-proven mechanisms, almost certainly proof-only.**

Two source facts settle it:
1. **`is_checkpoint` is a single bool with no provenance** — a scene-cut checkpoint and a grid checkpoint are byte-identical on the cache slot. The recovery search can't tell them apart.
2. **The recovery search selects first-present descending, regardless of checkpoint flag** — checkpoints aren't *preferred*, they just *survive eviction*, so in a cold region the surviving present frames *are* the checkpoints.

So "a scene-cut checkpoint found as a recovery anchor" = **P.11C.4** (scene cut creates checkpoint, proven) + **D.5** (checkpoint survives prune, proven) + **D.1/D.2** (exact-anchor recovery descends and anchors on a present checkpoint, proven). Because the code path is provenance-blind, **there's no new unit-level path to prove** — it's an end-to-end *integration* proof that the composition behaves, not a new mechanism.

What this means practically:
- **Likely no source change** (at most a small KDT-only observability field, if the recovery trace doesn't already print `anchor=<frame>` + a way to confirm the anchor is a checkpoint).
- **The real work is the harness**, and it's harder than the D-series in one specific way: it must deterministically *drive the live prune* — create a scene-cut checkpoint at frame C, evict C's non-checkpoint neighbours but retain C, then force a later cold recovery that descends to C. That needs a much larger synthetic clip than the 4-frame D-harnesses (the pools are sized for real workloads: ~110 non-checkpoint, 16/6 checkpoint).
- **Fallback framing flagged**: if driving the live prune from a `.vpy` proves too fragile, a cache-core selftest composing the proven primitives directly (store scene-checkpoint, prune neighbours, run bounded search, assert anchor) proves the same composition where eviction is deterministic. My lean: try the `.vpy` first (true end-to-end), fall back to selftest if prune-driving is fragile.

I deliberately did *not* over-engineer this — the analysis flags it as the arc's lightest subphase and routes the genuine open questions (minimum frame count to drive prune, whether the recovery KDT already shows the anchor, .vpy-vs-selftest reliability) to the coder as read-first questions Q1–Q4.

So the cadence: this is the **coder-read step** — same pattern as .3/.4. The coder reads the analysis, confirms Q4 (no behavioural change), and advises on Q1/Q2/Q3 (the harness-feasibility questions, which it's better placed to judge against the live pool sizes than I am from reconstructed source).

Want me to trim this to a relayable coder-read brief (the §7 questions + the §2 composition finding), or send the analysis as-is? And one steer worth your input since it affects the harness path: do you have a preference between the **end-to-end `.vpy`** (more convincing, but needs a big clip to drive prune) and the **selftest composition** (deterministic, but unit-level) — or shall we let the coder advise on which is reliably drivable first?